Just put

dinput8.dll
HoodiePatcher.dll
HoodiePatcher.ini
modengine.ini
Mod

in your dark souls 3 directory (Where the .exe is located)

it uses alternate savegame location by default, so if you want to change that. You can in modengine.ini. By default it's the ideal option.

Network access is blocked by default, so disable the "blockNetworkAccess" in modengine.ini if you want to play online.

It is likely not compatible with other mods that edit weapons.

 